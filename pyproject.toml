[project]
name = "byegpt"
version = "0.1.0"
description = "Export supported ChatGPT and OpenAI API data surfaces."
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
byegpt = "byegpt.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
